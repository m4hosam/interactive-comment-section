<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/style2.css">
    <link
        href="https://fonts.googleapis.com/css2?family=Kumbh+Sans:wght@400;700&family=Rubik:wght@400;500;700&display=swap"
        rel="stylesheet">
    <title>Comments</title>
</head>

<body>

    <div class="reply-card">
        <img src="images/avatars/image-<%= currentUsername %>.png" alt="">
        <form action="/reply" method="post">
            <textarea name="comment" id="replyed-comment">@user name, </textarea>
            <button>REPLY</button>
        </form>
    </div>

    <div class="container">
        <% for(let i=0 ;i < data.length ; i++){ %>
            <div class="basic-card">
                <div class="score">
                    <div id="add" class="controller">+</div>
                    <div id="score-value">
                        <%= data[i].score %>
                    </div>
                    <div id="minus" class="controller">–</div>
                </div>
                <div class="content">
                    <div class="card-header">
                        <img class="avatar" src="images/avatars/image-<%= data[i].username %>.png" alt="">
                        <h5>
                            <%= data[i].username %>
                        </h5>
                        <h6>
                            <%= data[i].createdAt %>
                        </h6>
                        <div class="reply">
                            <img src="images/icon-reply.svg" alt="">
                            Reply
                        </div>
                    </div>
                    <p>
                        <%= data[i].content %>
                    </p>
                </div>
            </div>
            <% } %>
                <div class="reply-card send-card">
                    <img src="images/avatars/image-<%= currentUsername %>.png" alt="">
                    <form action="/reply" method="post">
                        <textarea name="comment" id="replyed-comment"></textarea>
                        <button>SEND</button>
                    </form>
                </div>



    </div>
    <script src="js/app.js"></script>
</body>

</html>